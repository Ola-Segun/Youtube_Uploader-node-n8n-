{
  "name": "yt-uploader",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix src/frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix src/frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}